# 🎯 **Exact Step-by-Step Implementation Guide**

---

## **DAY 1: SETUP (4 hours)**

### **Task 1.1: Neon Database Setup (10 minutes)**

1. Go to https://neon.tech
2. Click "Sign Up" → Use GitHub/Google login
3. Click "Create Project"
4. Name: `university-management`
5. Region: Select closest to you
6. Click "Create Project"
7. **COPY** the connection string (looks like: `postgresql://user:pass@ep-xxx.neon.tech/dbname`)
8. Save it in a text file labeled `NEON_DATABASE_URL`

---

### **Task 1.2: Google Drive API Setup (20 minutes)**

1. Go to https://console.cloud.google.com
2. Click "Select a project" → "New Project"
3. Project name: `university-files`
4. Click "Create"
5. Wait 30 seconds, then select the project
6. Click "☰ Menu" → "APIs & Services" → "Library"
7. Search "Google Drive API" → Click it → Click "Enable"
8. Click "☰ Menu" → "APIs & Services" → "Credentials"
9. Click "+ CREATE CREDENTIALS" → "OAuth client ID"
10. If prompted, click "CONFIGURE CONSENT SCREEN"
    - User Type: External → Create
    - App name: `University Management`
    - User support email: Your email
    - Developer email: Your email
    - Click "Save and Continue" 3 times → "Back to Dashboard"
11. Back to Credentials → "+ CREATE CREDENTIALS" → "OAuth client ID"
12. Application type: "Web application"
13. Name: `django-backend`
14. Authorized redirect URIs: Add `http://localhost:8000/oauth2callback`
15. Click "Create"
16. **DOWNLOAD JSON** → Save as `google-credentials.json`
17. Click "☰ Menu" → "APIs & Services" → "Credentials"
18. Click "+ CREATE CREDENTIALS" → "Service account"
19. Service account name: `django-storage`
20. Click "Create and Continue" → "Done"
21. Click on the service account email you just created
22. Go to "Keys" tab → "ADD KEY" → "Create new key"
23. Key type: JSON → Create
24. **DOWNLOAD** this JSON file → Save as `service-account.json`

---

### **Task 1.3: Local Django Project Setup (30 minutes)**

**Open Terminal/Command Prompt:**

```bash
# Create project folder
mkdir university-management
cd university-management

# Create virtual environment
python -m venv venv

# Activate virtual environment
# On Windows:
venv\Scripts\activate
# On Mac/Linux:
source venv/bin/activate

# Create requirements.txt
cat > requirements.txt << EOF
Django==5.0
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.0
psycopg2-binary==2.9.9
Pillow==10.1.0
reportlab==4.0.7
python-dotenv==1.0.0
gunicorn==21.2.0
whitenoise==6.6.0
django-storages==1.14.2
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.111.0
EOF

# Install packages
pip install -r requirements.txt

# Create Django project
django-admin startproject config .

# Create apps
python manage.py startapp accounts
python manage.py startapp students
python manage.py startapp payments
python manage.py startapp academics

# Create .env file
cat > .env << EOF
DEBUG=True
SECRET_KEY=django-insecure-change-this-in-production-xyz123
DATABASE_URL=paste_your_neon_url_here
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173
EOF
```

**Copy your Neon URL into .env file**

---

### **Task 1.4: Configure Django Settings (20 minutes)**

**Open `config/settings.py` and AI prompt:**

```
Replace the entire config/settings.py file with a production-ready setup that includes:
1. Load environment variables from .env using python-dotenv
2. PostgreSQL database connection using DATABASE_URL from env
3. Django REST Framework with JWT authentication
4. CORS headers allowing localhost:5173
5. Google Drive storage backend configuration
6. Static files with WhiteNoise
7. Installed apps: rest_framework, corsheaders, accounts, students, payments, academics

Use this structure:
- SECRET_KEY from env
- DEBUG from env
- ALLOWED_HOSTS from env
- Database using dj-database-url
- REST_FRAMEWORK settings with JWT
- CORS settings
- Static/media files config
```

**Copy AI's response and paste into `config/settings.py`**

---

### **Task 1.5: Create Google Drive Storage Backend (15 minutes)**

**Create file: `config/storage_backends.py`**

**AI Prompt:**
```
Create a Django custom storage backend class for Google Drive using google-api-python-client.
The class should:
1. Read service account JSON from environment variable GOOGLE_DRIVE_CREDENTIALS_JSON
2. Authenticate using service account
3. Upload files to a specific folder (get folder ID from env)
4. Generate public URLs for uploaded files
5. Handle file deletion
6. Implement Django's Storage interface (_open, _save, delete, exists, url, size)
```

**Copy AI response → Save to `config/storage_backends.py`**

**Add to `.env`:**
```bash
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here
GOOGLE_DRIVE_CREDENTIALS_JSON=paste_service_account_json_content_here_as_single_line
```

**To get folder ID:**
1. Go to https://drive.google.com
2. Create a new folder: "University Files"
3. Open the folder
4. Copy the ID from URL: `drive.google.com/drive/folders/FOLDER_ID_HERE`
5. Paste into `.env`

---

### **Task 1.6: Initialize Git (5 minutes)**

```bash
git init
cat > .gitignore << EOF
venv/
__pycache__/
*.pyc
.env
*.sqlite3
db.sqlite3
.DS_Store
google-credentials.json
service-account.json
staticfiles/
media/
EOF

git add .
git commit -m "Initial Django setup"
```

---

### **Task 1.7: Railway Backend Deployment (15 minutes)**

1. Go to https://railway.app
2. Click "Start a New Project"
3. Login with GitHub
4. Click "Deploy from GitHub repo"
5. Click "Configure GitHub App"
6. Select your repositories → Install
7. Back to Railway → Select your repo
8. Railway auto-detects Django → Wait for build
9. Click on the deployment
10. Go to "Settings" tab
11. Click "Generate Domain" under "Networking"
12. **COPY** the URL (e.g., `your-app.up.railway.app`)
13. Go to "Variables" tab
14. Click "RAW Editor"
15. Paste this (replace with your values):

```bash
DEBUG=False
SECRET_KEY=generate-a-long-random-string-here-use-random-org
DATABASE_URL=your_neon_url_here
ALLOWED_HOSTS=your-app.up.railway.app,yourdomain.com
CORS_ALLOWED_ORIGINS=https://your-netlify-app.netlify.app
GOOGLE_DRIVE_FOLDER_ID=your_folder_id
GOOGLE_DRIVE_CREDENTIALS_JSON={"type":"service_account","project_id":"...paste the entire JSON as one line..."}
PORT=8000
```

16. Click "Save"
17. Wait for redeployment (2 minutes)

---

### **Task 1.8: Create React Frontend (20 minutes)**

**Open NEW terminal (keep Django terminal open):**

```bash
# Go back to project root
cd ..

# Create React project
npm create vite@latest frontend -- --template react
cd frontend

# Install dependencies
npm install
npm install axios react-router-dom recharts lucide-react

# Install Tailwind
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

**Edit `tailwind.config.js`:**
```javascript
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

**Edit `src/index.css`:**
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

**Test locally:**
```bash
npm run dev
```
Open http://localhost:5173 → You should see Vite + React page

---

### **Task 1.9: Netlify Frontend Deployment (10 minutes)**

1. Go to https://netlify.com
2. Click "Sign up" → Use GitHub
3. Click "Add new site" → "Import an existing project"
4. Click "Deploy with GitHub"
5. Authorize Netlify → Select your repo
6. Base directory: `frontend`
7. Build command: `npm run build`
8. Publish directory: `dist`
9. Click "Deploy site"
10. Wait 2 minutes
11. Click "Site settings" → "Change site name"
12. Name it: `university-management-xyz` (unique name)
13. **COPY** the URL: `https://university-management-xyz.netlify.app`

**Add environment variable to Netlify:**
1. Site settings → "Environment variables"
2. Add variable:
   - Key: `VITE_API_URL`
   - Value: `https://your-railway-app.up.railway.app/api`
3. Click "Save"
4. Go to "Deploys" → "Trigger deploy" → "Deploy site"

---

### **✅ Day 1 Complete Checklist:**
- [ ] Neon database URL saved
- [ ] Google Drive API enabled + credentials downloaded
- [ ] Django project created locally
- [ ] `.env` file configured
- [ ] Git initialized
- [ ] Railway deployment working
- [ ] React app created
- [ ] Netlify deployment working

**Test:** Visit your Railway URL → Should see Django error page (that's OK for now)

---

## **DAY 2: DJANGO MODELS & API (8 hours)**

### **Task 2.1: Create Database Models (90 minutes)**

**File: `accounts/models.py`**

**AI Prompt:**
```
Create Django User model extending AbstractUser with:
- role field (choices: ADMIN, TEACHER, STUDENT)
- phone_number
- address
- profile_picture (ImageField uploaded to 'profiles/')
- is_active boolean

Also create a Student model with:
- user (OneToOne to User)
- student_id (unique, auto-generated like STU2024001)
- date_of_birth
- guardian_name
- guardian_phone
- admission_date
- batch (ForeignKey to Batch)
- photo (ImageField to 'students/photos/')
- blood_group (choices)
- present_address
- permanent_address

Include __str__ methods and Meta classes with ordering
```

**Copy response → Paste into `accounts/models.py`**

---

**File: `students/models.py`**

**AI Prompt:**
```
Create Django models for:

1. Course model:
   - name, code (unique), description, duration_months, fee

2. Batch model:
   - name (like "HSC 2024"), course (FK), start_date, end_date, is_active

3. Enrollment model:
   - student (FK to Student), batch (FK), enrollment_date, status (choices: active, completed, dropped)
   - unique_together on student and batch

4. Teacher model:
   - user (OneToOne)
   - employee_id (unique, auto like TCH2024001)
   - subjects (TextField)
   - qualification
   - joining_date
   - monthly_salary

Include proper __str__ and Meta classes
```

**Copy → Paste into `students/models.py`**

---

**File: `payments/models.py`**

**AI Prompt:**
```
Create Django models for:

1. FeeStructure:
   - batch (FK), fee_type (choices: tuition, exam, admission), amount, due_date

2. Payment:
   - student (FK), fee_structure (FK), amount_paid, payment_date
   - payment_method (choices: cash, bank_transfer, online)
   - transaction_id, discount_amount, remarks
   - created_at, updated_at (auto)
   - Method: calculate_due_amount()

3. Expense:
   - expense_type (choices: salary, rent, utility, maintenance, other)
   - amount, description, expense_date, paid_to
   - created_by (FK to User)
   - receipt_file (FileField optional)

Include __str__ and proper ordering
```

**Copy → Paste into `payments/models.py`**

---

**File: `academics/models.py`**

**AI Prompt:**
```
Create Django models for:

1. Subject:
   - name, code (unique), course (FK), total_marks

2. Exam:
   - name (midterm, final, SSC, HSC), batch (FK), exam_date, total_marks

3. Result:
   - student (FK), exam (FK), subject (FK), marks_obtained
   - grade (calculated property based on marks)
   - remarks
   - unique_together on student, exam, subject
   - Method: calculate_grade() returns A+, A, B, etc

Include __str__ methods
```

**Copy → Paste into `academics/models.py`**

---

### **Task 2.2: Register Models in Admin (15 minutes)**

**For each app, edit `admin.py`:**

**AI Prompt:**
```
Create Django admin configuration for all models in [accounts/students/payments/academics] app.
For each model:
- Register with @admin.register decorator
- Add list_display with relevant fields
- Add search_fields
- Add list_filter
- Add readonly_fields for auto-generated IDs and dates
- Use list_select_related for ForeignKeys
```

**Execute for each:**
- `accounts/admin.py`
- `students/admin.py`
- `payments/admin.py`
- `academics/admin.py`

---

### **Task 2.3: Run Migrations (10 minutes)**

```bash
# In Django terminal
python manage.py makemigrations
python manage.py migrate

# Create superuser
python manage.py createsuperuser
# Username: admin
# Email: your@email.com
# Password: (create strong password)

# Run server
python manage.py runserver
```

**Test:** Go to http://localhost:8000/admin → Login → You should see all models

---

### **Task 2.4: Create API Serializers (60 minutes)**

**File: `accounts/serializers.py`** (create new file)

**AI Prompt:**
```
Create DRF serializers for User and Student models:
1. UserSerializer - all fields except password
2. StudentSerializer - nested user data, all student fields
3. StudentCreateSerializer - for creating students with user
4. LoginSerializer - username/email and password validation
5. ChangePasswordSerializer

Use ModelSerializer, include validation
```

**Repeat for other apps:**
- `students/serializers.py` → Course, Batch, Enrollment, Teacher serializers
- `payments/serializers.py` → Payment, FeeStructure, Expense serializers
- `academics/serializers.py` → Subject, Exam, Result serializers

**For each, prompt:**
```
Create DRF ModelSerializers for all models in [app_name].
Include nested relations where appropriate (use depth=1).
Add custom validation methods where needed.
```

---

### **Task 2.5: Create API ViewSets (60 minutes)**

**File: `accounts/views.py`**

**AI Prompt:**
```
Create DRF ViewSets for User and Student:
1. UserViewSet with CRUD, permission: IsAuthenticated
2. StudentViewSet with:
   - list, retrieve, create, update, destroy
   - Filter by batch, admission_date
   - Search by name, student_id
   - Ordering by admission_date
3. LoginView using simplejwt
4. ChangePasswordView

Use ModelViewSet, add filterset_fields, search_fields, ordering_fields
```

**Repeat for:**
- `students/views.py` → Course, Batch, Enrollment, Teacher viewsets
- `payments/views.py` → Payment, Expense viewsets + custom stats endpoint
- `academics/views.py` → Subject, Exam, Result viewsets + grade calculation

---

### **Task 2.6: Configure URLs (30 minutes)**

**File: `config/urls.py`**

**AI Prompt:**
```
Create main URLs configuration:
1. Include admin
2. Include rest_framework URLs
3. Include JWT token endpoints (token, refresh, verify)
4. Create API router and register all viewsets from accounts, students, payments, academics
5. Include router URLs under /api/

Use DefaultRouter from rest_framework
```

**Copy → Replace `config/urls.py`**

---

**Create API URLs for each app:**

**`accounts/urls.py`** (create new):
```python
from rest_framework.routers import DefaultRouter
from .views import UserViewSet, StudentViewSet

router = DefaultRouter()
router.register('users', UserViewSet)
router.register('students', StudentViewSet)

urlpatterns = router.urls
```

**Repeat for students, payments, academics apps**

---

### **Task 2.7: Test API Locally (20 minutes)**

```bash
# Run server
python manage.py runserver
```

**Install REST Client (VS Code) or use Postman:**

**Test endpoints:**
1. GET http://localhost:8000/api/students/ → Should return empty list
2. POST http://localhost:8000/api/token/
   ```json
   {
     "username": "admin",
     "password": "your_password"
   }
   ```
   → Should return access and refresh tokens

3. GET http://localhost:8000/api/students/ with Authorization header:
   `Bearer your_access_token`

**If all work → Continue**

---

### **Task 2.8: Deploy to Railway (15 minutes)**

```bash
# Commit changes
git add .
git commit -m "Add models, serializers, views"
git push origin main
```

**Railway auto-deploys** (wait 3 minutes)

**Run migrations on Railway:**
1. Go to Railway dashboard
2. Click your project → Click deployment
3. Click "Settings" → "Deploy"
4. Or use Railway CLI:

```bash
# Install Railway CLI
npm install -g @railway/cli

# Login
railway login

# Link project
railway link

# Run migrations
railway run python manage.py migrate

# Create superuser
railway run python manage.py createsuperuser
```

---

### **✅ Day 2 Complete Checklist:**
- [ ] All models created and migrated
- [ ] Django admin shows all models
- [ ] All serializers created
- [ ] All viewsets created
- [ ] API endpoints accessible
- [ ] JWT authentication working
- [ ] Railway deployment updated

**Test:** Visit `https://your-app.railway.app/admin` → Should see admin login

---

## **DAY 3: REACT FRONTEND BASE (8 hours)**

### **Task 3.1: Project Structure Setup (30 minutes)**

```bash
cd frontend/src

# Create folder structure
mkdir -p components/layout
mkdir -p components/auth
mkdir -p components/students
mkdir -p components/payments
mkdir -p components/academics
mkdir -p pages
mkdir -p services
mkdir -p utils
mkdir -p context
```

---

### **Task 3.2: API Service Setup (20 minutes)**

**File: `frontend/src/services/api.js`**

**AI Prompt:**
```
Create an axios instance for React with:
1. Base URL from import.meta.env.VITE_API_URL
2. Request interceptor that adds Authorization header from localStorage token
3. Response interceptor that handles 401 errors (redirect to login)
4. Export the axios instance and helper functions:
   - login(username, password)
   - register(data)
   - refreshToken()
   - logout()
```

**Copy → Save to `frontend/src/services/api.js`**

---

**Create `.env` in frontend folder:**
```bash
VITE_API_URL=http://localhost:8000/api
```

---

### **Task 3.3: Auth Context (30 minutes)**

**File: `frontend/src/context/AuthContext.jsx`**

**AI Prompt:**
```
Create React Context for authentication:
1. AuthProvider component with state: user, token, loading
2. Methods: login, logout, register
3. useEffect to check token in localStorage on mount
4. Provide context value with user, token, login, logout, isAuthenticated
5. Export useAuth custom hook

Store token in localStorage, decode JWT to get user info
```

**Copy → Save**

---

### **Task 3.4: Protected Route Component (15 minutes)**

**File: `frontend/src/components/auth/ProtectedRoute.jsx`**

**AI Prompt:**
```
Create ProtectedRoute component:
- Use useAuth hook
- If not authenticated, redirect to /login using Navigate
- Show loading spinner while checking auth
- Otherwise render children
```

---

### **Task 3.5: Login Page (45 minutes)**

**File: `frontend/src/pages/LoginPage.jsx`**

**AI Prompt:**
```
Create Login page component with Tailwind CSS:
1. Form with username and password fields
2. Submit handler that calls login from useAuth
3. Error message display
4. Link to register page
5. Modern design with gradient background
6. Centered card layout
7. Loading state on submit button

Use lucide-react for icons
```

**Copy → Save**

---

### **Task 3.6: Dashboard Layout (60 minutes)**

**File: `frontend/src/components/layout/Sidebar.jsx`**

**AI Prompt:**
```
Create Sidebar component with Tailwind:
1. Logo at top
2. Navigation links based on user role:
   - Admin: Dashboard, Students, Teachers, Payments, Expenses, Results
   - Teacher: Dashboard, Students, Results
   - Student: Dashboard, My Results, Payments
3. Active link highlighting
4. Logout button at bottom
5. User info display (name, role)
6. Responsive (collapsible on mobile)

Use lucide-react icons: Home, Users, DollarSign, BookOpen, LogOut
```

---

**File: `frontend/src/components/layout/DashboardLayout.jsx`**

**AI Prompt:**
```
Create DashboardLayout component:
1. Flex layout with Sidebar on left
2. Main content area on right
3. Top navbar with search, notifications, profile dropdown
4. Renders {children} in main area
5. Responsive design
```

---

### **Task 3.7: Router Setup (30 minutes)**

**File: `frontend/src/App.jsx`**

**AI Prompt:**
```
Create App component with React Router:
Routes:
- / → Dashboard (protected)
- /login → LoginPage
- /register → RegisterPage
- /students → StudentsPage (protected, admin only)
- /students/:id → StudentDetailPage (protected)
- /payments → PaymentsPage (protected)
- /results → ResultsPage (protected)

Wrap protected routes in ProtectedRoute and DashboardLayout
```

---

### **Task 3.8: Dashboard Home Page (45 minutes)**

**File: `frontend/src/pages/DashboardPage.jsx`**

**AI Prompt:**
```
Create Dashboard home page with:
1. Stats cards (Total Students, Revenue This Month, Pending Payments, Teachers)
2. Recent payments table
3. Upcoming exams list
4. Revenue chart (Recharts LineChart)
5. Use Tailwind grid layout
6. Fetch data from API using useEffect

Use lucide-react icons for stats cards
```

---

### **Task 3.9: Students List Page (60 minutes)**

**File: `frontend/src/pages/StudentsPage.jsx`**

**AI Prompt:**
```
Create Students list page:
1. Search bar (filter by name, student_id)
2. Filter dropdown (by batch)
3. Students table with columns: Photo, Name, Student ID, Batch, Phone, Actions
4. Pagination (20 per page)
5. Add Student button (opens modal)
6. Edit/Delete buttons per row
7. Fetch students from API on mount
8. Loading skeleton while fetching

Use Tailwind table styling
```

---

**File: `frontend/src/components/students/AddStudentModal.jsx`**

**AI Prompt:**
```
Create modal component for adding student:
1. Form fields: name, email, phone, date_of_birth, guardian_name, guardian_phone, batch, photo upload
2. Photo preview before upload
3. Submit handler that posts to API
4. Close modal on success
5. Show success/error toast
6. Form validation

Use Tailwind modal (fixed overlay with centered card)
```

---

### **Task 3.10: Test & Deploy (30 minutes)**

```bash
# Test locally
npm run dev
```

**Check:**
- [ ] Login page loads
- [ ] Can login with admin credentials
- [ ] Dashboard shows after login
- [ ] Sidebar navigation works
- [ ] Students page loads (empty is OK)

**Deploy to Netlify:**
```bash
git add .
git commit -m "Complete frontend base"
git push origin main
```

Netlify auto-deploys (2 minutes)

---

### **✅ Day 3 Complete Checklist:**
- [ ] API service configured
- [ ] Auth context working
- [ ] Login page functional
- [ ] Dashboard layout complete
- [ ] Sidebar navigation working
- [ ] Students list page shows
- [ ] Netlify deployment updated

---

## **DAYS 4-10: Rapid Format**

I'll give you the exact file list and AI prompts for each remaining day. You'll follow the same pattern: **Create file → Ask AI → Copy → Test**

---

## **DAY 4: PAYMENT SYSTEM**

### **Files to Create:**
1. `frontend/src/pages/PaymentsPage.jsx`
2. `frontend/src/components/payments/AddPaymentModal.jsx`
3. `frontend/src/components/payments/PaymentHistory.jsx`

### **AI Prompts:**

**PaymentsPage.jsx:**
```
Create Payments page with:
- Filter by student (searchable dropdown), date range, payment status
- Payments table: Student Name, Amount, Payment Date, Type, Status, Actions
- Add Payment button
- Total revenue display
- Export to CSV button
- Pagination
```

**AddPaymentModal.jsx:**
```
Create payment form modal:
- Select student (async searchable dropdown)
- Fee type (tuition/exam)
- Amount (auto-fill from fee structure)
- Discount field
- Payment method
- Transaction ID
- Calculate final amount
- Submit to POST /api/payments/
```

**Backend Task:**
Add to `payments/views.py`:
```
Add custom action to PaymentViewSet:
@action(detail=False, methods=['get'])
def statistics(self, request):
    # Calculate total revenue, pending payments, monthly breakdown
    # Return JSON with stats
```

---

## **DAY 5: EXPENSE TRACKING**

### **Files:**
1. `frontend/src/pages/ExpensesPage.jsx`
2. `frontend/src/components/expenses/AddExpenseModal.jsx`
3. `frontend/src/components/expenses/SalaryManagement.jsx`

### **AI Prompts:**

**ExpensesPage.jsx:**
```
Create Expenses page:
- Filter by type, date range
- Expenses table: Type, Amount, Description, Date, Paid To
- Add Expense button
- Monthly summary chart (Recharts BarChart)
- Teacher salary section (separate component)
```

**SalaryManagement.jsx:**
```
Create teacher salary component:
- List of teachers with monthly salary
- Mark as paid button per month
- Payment history per teacher
- Total salary expense display
```

---

## **DAY 6: RESULTS SYSTEM**

### **Files:**
1. `frontend/src/pages/ResultsPage.jsx`
2. `frontend/src/components/academics/AddResultModal.jsx`
3. `frontend/src/components/academics/BulkResultUpload.jsx`

### **AI Prompts:**

**ResultsPage.jsx:**
```
Create Results page:
- Filter by exam, batch, student
- Results table: Student, Subject, Marks, Grade
- Add Result button
- Bulk Upload button (CSV)
- Class statistics (average, pass rate)
```

**BulkResultUpload.jsx:**
```
Create CSV upload component:
- File input for CSV
- Preview table before upload
- Validate format (student_id, subject_code, marks)
- Batch create results
- Show success/error count
```

**Backend:**
Add to `academics/views.py`:
```
Create ResultViewSet action for bulk upload:
@action(detail=False, methods=['post'])
def bulk_upload(self, request):
    # Parse CSV from request.FILES
    # Validate each row
    # Create Result objects
    # Return success/error report
```

---

## **DAY 7: REPORT CARDS**

### **Backend File:** `academics/utils.py`

**AI Prompt:**
```
Create function generate_report_card(student_id, exam_id):
- Use ReportLab to create PDF
- Include: Student info, photo, exam name, subject-wise marks, grades
- Calculate total, percentage, GPA
- Add school logo, watermark
- Return PDF as response
```

**Add view in `academics/views.py`:**
```
@action(detail=True, methods=['get'])
def report_card(self, request, pk=None):
    result = self.get_object()
    pdf = generate_report_card(result.student.id, result.exam.id)
    return FileResponse(pdf, filename='report_card.pdf')
```

### **Frontend File:** `frontend/src/components/academics/ReportCardViewer.jsx`

**AI Prompt:**
```
Create component to view/download report card:
- Select student and exam
- Preview button (opens PDF in new tab)
- Download button
- Print button
- Bulk download for whole class
```

---

## **DAY 8: STUDENT PORTAL**

### **Files:**
1. `frontend/src/pages/student/StudentDashboard.jsx`
2. `frontend/src/pages/student/MyResults.jsx`
3. `frontend/src/pages/student/MyPayments.jsx`

**AI Prompt for each:**
```
Create student-specific [Dashboard/Results/Payments] page:
- Read-only view
- Only show current student's data
- Modern card-based layout
- Download buttons for reports
```

---

## **DAY 9: POLISH**

### **Tasks:**
1. Add loading skeletons to all pages
2. Add toast notifications (use react-hot-toast)
3. Add confirmation dialogs before delete
4. Make all tables responsive (mobile cards view)
5. Add form validation everywhere
6. Add error boundaries

**AI Prompt:**
```
Improve [component name] with:
- Loading skeleton while fetching data
- Error boundary wrapper
- Toast notifications for actions
- Confirmation dialog before delete
- Mobile responsive design
- Form validation with error messages
```

**Apply to each major component**

---

## **DAY 10: DEPLOY & TEST**

### **Backend Production Checklist:**

**File: `config/settings.py`**
```python
# Change these for production
DEBUG = os.getenv('DEBUG', 'False') == 'True'
ALLOWED_HOSTS = os.getenv('ALLOWED_HOSTS', '').split(',')
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

**Create: `Procfile`** (in root)
```
web: gunicorn config.wsgi --log-file -
```

**Create: `runtime.txt`**
```
python-3.11.5
```

**Railway:**
```bash
git add .
git commit -m "Production ready"
git push origin main

# Wait for deploy, then:
railway run python manage.py migrate
railway run python manage.py collectstatic --noinput
railway run python manage.py createsuperuser
```

---

### **Frontend Production:**

**Update `.env.production` in frontend:**
```
VITE_API_URL=https://your-app.railway.app/api
```

**Netlify:**
```bash
cd frontend
npm run build
# Test production build locally:
npm run preview

# Push to deploy
git add .
git commit -m "Production build"
git push origin main
```

---

### **Final Testing:**

1. **Create test data via Django admin:**
   - 10 students
   - 2 courses
   - 3 batches
   - 5 payments
   - Some results

2. **Test all flows:**
   - [ ] Login as admin → See dashboard
   - [ ] Add a student → Upload photo → Save
   - [ ] Record a payment → See in payments list
   - [ ] Enter results → Generate report card → Download PDF
   - [ ] Login as student → See own data only
   - [ ] Check mobile responsiveness

3. **Performance:**
   - [ ] All pages load < 3 seconds
   - [ ] Images load properly from Google Drive
   - [ ] No console errors

---

## **🎯 Summary: Your Exact Workflow**

**Every task follows this pattern:**

1. **Read the file name and AI prompt above**
2. **Create the file in your editor**
3. **Copy the AI prompt → Paste into Claude/Cursor/ChatGPT**
4. **Copy AI's response → Paste into the file**
5. **Save → Test locally**
6. **If error → Copy error message → Ask AI "Fix this error: [paste]"**
7. **When working → Commit → Push (auto-deploys)**

**Use this exact sentence when stuck:**
```
"I'm getting this error: [paste error]
My code: [paste code]
Fix it step by step"
```

---

### **Emergency Shortcuts:**

**If Day 2-3 takes too long:**
- Use Django admin for EVERYTHING initially
- Skip custom frontend for now
- Just build student portal (Days 8-9)
- You can add admin UI later

**If Google Drive is too complex:**
- Just use Cloudinary free tier (10GB)
- Change one line in settings.py
- Skip all Google Drive setup

**If running out of time:**
Priority order:
1. ✅ Backend API working
2. ✅ Django admin functional
3. ✅ Student login + view results
4. ⏭️ Skip: Advanced reports, analytics, bulk uploads

---

**Start with Day 1 Task 1.1 right now. Don't overthink. Copy-paste-test. Ship in 10 days.** 🚀